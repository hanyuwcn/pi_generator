class PIWriter:
    """
    Write content(header, quote and footer) into an output.

    This module does NOT involve any business related functionality;
    it only handles the placement and format of components.

    Each company and product-line has its own strategy.

    Input: dictionaries of header, quote and footer
    Output: a requested invoice that proper lays out all information
    """
    def write_to_output(self, header, quote, footer):
        """
        Write all desired content of the invoice into the output file.

        :param header: of the PI
        :param quote: of the PI
        :param footer: of the PI
        """
        pass